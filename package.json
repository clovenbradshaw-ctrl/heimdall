{
  "name": "heimdall",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Turn any phone or laptop into a managed compute node. Matrix room for signaling, WebRTC for the data, WebLLM for local inference.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "icons": "node scripts/gen-icons.mjs"
  },
  "dependencies": {
    "@mlc-ai/web-llm": "^0.2.85",
    "matrix-js-sdk": "^42.4.0"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}